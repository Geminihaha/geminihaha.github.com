# Zombie Hunter — 개발 기록

## 개요
뱀파이어 서바이버 스타일의 웹 게임. 순수 HTML + Canvas + JavaScript로 제작됨.

- **원본 출처**: Ghost Hero 레포지토리 내 prototype
- **플레이**: `zombiehunter/index.html` 브라우저 열기
- **빌드 없음**: 정적 HTML+JS, 번들러 불필요

---

## 수정 내역

### 2026-07-06
- 최초 게임 프로토타이핑: 7개 캐릭터, 7개 무기(채찍/마법봉/단검/번개링/화염지팡이/도끼/마법서), 몬스터 6종(해골/박쥐/유령/스켈레톤 아처/늑대인간/미라), 레벨업 강화 시스템(12종), 맵 4000x4000, 카메라 follow 및 그리드 배경 구현.

### 2026-07-07 10:58 (약) — 채찍(Whip) 공격 히트 판정 버그 수정
- **증상**: 1번 캐릭터(안토니오) 자동 공격 시 범위 호(arc)는 시각적으로 표시되나, 호 안에 몬스터가 들어가도 데미지를 전혀 주지 못함.
- **원인 분석**: `WEAPON_DEFS.whip.update()` 내 `hit:` 함수에서 거리(`dist < r`) 및 각도(`Math.abs(d) < arc/2+0.2`) 조건이 있었으나, 각도 계산(`d = a - angle`)과 시각적 호 생성 방식(`ctx.arc`) 간 정합성 문제로 피격 판정 누락.
- **수정 사항**:
  - `hit()` 함수 재작성: 거리(90px) 조기 탈출 후, 각도 차이(`a - angle`)를 정규화하여 판정.
  - 진단 도구 적용 및 제거: `return true`(무조건 명중) 테스트, 화면 흔들림(`g.screenshake=12`), 히트 카운터 HUD(`히트:N`), 데미지 팝업 강화(22px 볼드+테두리).
  - 버그 복구: `arc/r` 변수 선언이 삭제되었던 문제 해결.
- **검증**: 모바일 환경 정상 작동 확인, 모든 무기 피격 판정 확인.

### 2026-07-07 14:23 (약) — 모바일 화면 최적화 (리사이징)
- **개요**: 모바일 기기별 다양한 해상도에 대응하기 위해 캔버스 크기를 동적으로 조정.
- **주요 수정**:
  - `game.js`: `window.innerWidth`/`innerHeight`를 감지하여 캔버스 크기를 즉시 반영하는 `resize()` 함수 구현 및 `window.addEventListener('resize', resize)` 추가.
  - `window.onload` 시 `resize()`를 호출하도록 초기화 로직 보완.
  - 캔버스 CSS를 화면 꽉 차게 설정하여 모바일 브라우저에서 게임 화면이 작게 표시되거나 깨지는 문제 해결.

### 2026-07-07 15:40 (약) — 모바일 UI 레이아웃 최적화
- **개요**: 모바일 화면 리사이징 적용 이후, 메인 메뉴 및 레벨업 선택 화면의 UI 요소가 고정 좌표로 인해 화면을 벗어나거나 가려지는 현상 해결.
- **주요 수정**:
  - `renderMenu()`: 캐릭터 선택 박스들의 가로 폭(`menuBoxW`)을 화면 너비의 90%로 제한하고, 동적으로 중앙 정렬 배치되도록 로직 변경.
  - `renderLevelUp()`: 레벨업 시 나타나는 스킬 선택 카드 박스의 폭(`boxW`)을 화면 너비에 맞춰 유동적으로 축소(`Math.min(250, (W - 40) / count)`)하고 폰트 크기 및 간격을 최적화하여 좁은 화면에서도 가려짐 없이 모두 표시되도록 개선.

### 2026-07-07 17:30 (약) — 캐릭터 선택 후 화면 멈춤 현상 수정
- **증상**: 리사이징 적용 후 캐릭터 선택 시 게임 화면으로 전환되지 않고 멈춤.
- **원인**: 동적 해상도(`W`, `H` 변경)로 인해 게임 엔진 좌표계와 렌더링 루틴이 충돌하여 프레임이 중단됨.
- **수정 사항**: `resize()` 함수에서 `W`, `H`를 업데이트할 때 `game.camX`, `game.camY`도 함께 보정하여 엔진 좌표계가 깨지지 않도록 안정화.

### 2026-07-07 18:00 (약) — 메인 메뉴 입력 누락 현상 수정
- **증상**: 메인 화면에서 1초 이상 머무른 후 캐릭터 선택 터치가 동작하지 않음.
- **원인 분석**: `mouse.justClicked` 플래그를 게임 루프 하단에서 매 프레임 초기화하여, 터치 이벤트가 프레임 간극에 발생하면 플래그가 소비되기 전에 리셋됨 (레이스 컨디션). 또한 게임 루프 기반 입력 처리(`updateMenu` 내 hover + justClicked 체크)는 `requestAnimationFrame` 타이밍에 의존하므로 모바일 환경에서 미세한 지연이 발생하면 입력이 유실됨.
- **수정 사항**:
  - 캐릭터 선택 로직을 게임 루프(`updateMenu()`)에서 **완전히 분리**하여 이벤트 핸들러(`touchstart`, `mousedown`, `click`)에서 직접 처리하도록 변경.
  - `handleMenuClick()` 함수 추가: 터치/클릭 좌표를 캔버스 좌표로 변환한 후, 캐릭터 목록 상자와 비교하여 일치하면 즉시 `startGame()` 호출.
  - 게임 루프의 전역 `mouse.justClicked=false` 제거 (중복/조기 리셋 방지).
- **결과**: 터치 이벤트 발생과 동시에 메뉴 선택이 처리되므로, 프레임 타이밍이나 1초 이상 대기 후에도 항상 정상 동작.

### 2026-07-07 19:00 (약) — 리사이즈 이벤트 크래시 수정 + 터치 디버그 오버레이
- **증상**: 메뉴 화면에서 모바일 브라우저 리사이즈 이벤트(주소표시줄 숨김 등) 발생 시 `TypeError: Cannot read property 'x' of null` 발생 → 캔버스 초기화로 메뉴 깜빡임.
- **원인**: `resize()` 함수에서 `game` 객체 존재 여부만 확인하고 `game.player` null 여부를 확인하지 않음. 메뉴 상태에서는 `game.player=null`이므로 크래시.
- **수정 사항**:
  - `resize()` 조건을 `if(game)` → `if(game && game.player)`로 변경하여 메뉴 상태에서 크래시 방지.
  - 메뉴 화면 상단에 디버그 정보(`W/H/mouse 좌표`) 표시 추가 (진단용, 추후 제거 가능).
  - 터치 시 위치에 빨간 점 표시 추가 (터치 좌표 정확도 확인용).
